# UTS Plugin Sample

Copy the sample plugin into `UTSPlugins` to verify the automatic Pod integration flow:

```sh
cp -R UTSPlugins-Sample/uni-uts-offline-sample UTSPlugins/
pod install
```

Then open `HBuilder-Hello.xcworkspace`.
